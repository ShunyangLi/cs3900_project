import { SidebarRecommdDirective } from './sidebar-recommd.directive';

describe('SidebarRecommdDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarRecommdDirective();
    expect(directive).toBeTruthy();
  });
});
