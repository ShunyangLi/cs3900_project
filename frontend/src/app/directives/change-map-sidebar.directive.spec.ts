import { ChangeMapSidebarDirective } from './change-map-sidebar.directive';

describe('ChangeMapSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeMapSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
