export class CheckAvaData {
  constructor(
      children: string,
      adult: string,
      // tslint:disable-next-line:variable-name
      check_in: string,
      // tslint:disable-next-line:variable-name
      check_out: string,
      // tslint:disable-next-line:variable-name
      hotel_id: string
  ) {}
}
