export class RoomInfo {
  constructor(
    adults: string,
    bathroom: string,
    bedroom: string,
    children: string,
    // tslint:disable-next-line:variable-name
    hotel_id: string,
    // tslint:disable-next-line:variable-name
    img_url: Array<string>,
    name: string,
    price: string,
    // tslint:disable-next-line:variable-name
    room_id: string
  ) {}
}
